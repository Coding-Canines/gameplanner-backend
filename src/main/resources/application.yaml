ktor:
  application:
    modules:
      - com.codingcanines.ApplicationKt.module
  deployment:
    port: 8080
  # noinspection KtorYamlConfig
  development: ${DEV:false}

jwt:
  secret: $JWT_SECRET
  issuer: $JWT_ISSUER
  audience: $JWT_AUDIENCE
  realm: "Access to Game Planner API"
